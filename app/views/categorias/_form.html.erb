<%= form_for(categoria) do |f| %>
  <%= render 'errores', partial: true, model: categoria, model_name: 'La categoría' %>

  <div class="form-group">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion, class: 'form-control' %>
  </div>

  <div class="checkbox">
    <%= f.label :habilitado do %>
      <%= f.check_box :habilitado %>
      Habilitado
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-default', role: 'button'%>
    <%= link_to 'Cancelar', notas_path, class: 'btn btn-default', role: 'button' %>
  </div>
<% end %>
